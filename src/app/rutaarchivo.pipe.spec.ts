import { RutaarchivoPipe } from './rutaarchivo.pipe';

describe('RutaarchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new RutaarchivoPipe();
    expect(pipe).toBeTruthy();
  });
});
