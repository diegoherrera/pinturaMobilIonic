import { FormatCheckPipe } from './format-check.pipe';

describe('FormatCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
